description = "Run the test-gated experiment loop in the current worktree (wfw auto)"
prompt = """
Run workflowWrapper auto with objective: {{args}}

Execute:
!{wfw auto {{args}}}

Loop: one agent change per iteration, keep or revert by tests.
Stops when .wfw/auto/DONE exists and tests pass (default cap 12 iterations).
"""
